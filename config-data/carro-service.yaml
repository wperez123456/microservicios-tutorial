server:
  port: 8002
spring:
  application:
    name: carro-service
  datasource:
    url: jdbc:h2:file:./data/carrosdb
    driverClassName: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update