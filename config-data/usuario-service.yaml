server:
  port: 8001
spring:
  application:
    name: usuario-service
  datasource:
    url: jdbc:h2:file:./data/usuariosdb
    driverClassName: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update