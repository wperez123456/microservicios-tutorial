server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: gateway
          uri: https://github.com/wperez123456/microservicios-tutorial
          search-paths: config-data
  application:
    name: config-service
